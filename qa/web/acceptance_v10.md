# Three.js V0 / v10 验收

日期：2026-09-17。范围：新版圆润短腿四足资产与用户已确认的 V0 网页；未向外发布。

- 实际 Chrome 加载 v10 GLB、贴图、32 骨骼及 12 动作，截图 `desktop_v10.png`。
- v9/v10 侧面比较弹窗已打开并检查；截图 `comparison_v9_v10.png`。
- 15 秒画布演示已实际录制，原始文件 14.838 秒；导出为兼容播放的 H.264/30 fps。抽查 3.5、5.5、7.4、8.8、11.6、12.8 秒，确认侧看、抬头、伸爪、腾空及落地阶段。未录制屏幕或麦克风。
- 实际操作调试目标 X=0.35、Z=0.40，点击小跑；抵达位置 X=0.34、Z=0.39 后返回待机，符合 0.012 m 停止半径。
- 1024×768 横屏与 820×1180 竖屏布局已截图检查；临时浏览器尺寸已恢复。
- 运行检查中看到约 66–79 FPS，较小视口曾到 115 FPS；这些只是本机同时渲染任务下的观察值，不代表 iPad 性能。
- 实际 GLB 控制器测试 5 项通过：12 动作与 32 骨骼映射、归一化权重、跳跃序列与中断、追踪不累加、移动到点和快速动作切换。生产构建成功。Three.js 核心包约 574 KB，有打包体积提示。
- 在模型复制到网页目录之前，开发页面短暂出现配置缺失错误；复制完成并刷新后成功加载，此后未观察到新的运行错误。

输入适配、头颈限幅、身体跟随、移动与转身、动作过渡及调试开关完整保留。没有加入 Hover、Rapier、AI、自主行为或运行时 IK。配置明确眼球、眨眼、口型能力为 false。

没有实际 iPad Safari 验收；响应式截图不等于触摸手感、加载速度或设备性能验证。旧版截图和诊断影片保留但不作为 v10 验收结果。
